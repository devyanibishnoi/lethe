services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: lethe_user
      POSTGRES_PASSWORD: lethe_pass
      POSTGRES_DB: lethe_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: